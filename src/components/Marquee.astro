---
export interface Props {
  items: readonly string[]
}

const { items } = Astro.props
---

<div class="band">
  <ul class="sr-only">{items.map((item) => <li>{item}</li>)}</ul>
  <div class="marquee-track" aria-hidden="true">
    {[0, 1].map(() => (
      <div class="run">
        {items.map((item) => (
          <>
            <span class="item">{item}</span><span class="dot">◆</span>
          </>
        ))}
      </div>
    ))}
  </div>
</div>

<style>
  .band {
    position: relative;
    display: flex;
    align-items: center;
    height: 48px;
    overflow: hidden;
    border-top: 1px solid var(--border-gold-25);
    border-bottom: 1px solid var(--border-gold-25);
    background: rgba(7, 6, 4, 0.6);
  }
  .run {
    display: flex;
    align-items: center;
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 12px;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    white-space: nowrap;
    color: var(--color-gold);
  }
  .item {
    padding: 0 18px;
  }
  .dot {
    color: rgba(212, 175, 55, 0.4);
  }
  @media (min-width: 1024px) {
    .band {
      height: 64px;
    }
    .run {
      font-size: 15px;
    }
    .item {
      padding: 0 28px;
    }
  }
</style>
